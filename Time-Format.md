## Convert String date to ISO 8601

```java
		/*DateFormat originalFormat = new SimpleDateFormat("yyyy-MM-dd",Locale.US);
        DateFormat targetFormat = new SimpleDateFormat("yyyy-MM-dd'T'HH:mm:ss.SSSX", Locale.US);
        Date date = null;
        TimeZone etTimeZone = TimeZone.getTimeZone("GMT+4");
        targetFormat.setTimeZone( etTimeZone );
		try {
			date = originalFormat.parse(invoiceDate);
		} catch (ParseException e1) {
			// TODO Auto-generated catch block
			e1.printStackTrace();
		}
		//targetFormat.setTimeZone(TimeZone.getTimeZone("ETC"));
        String formattedDate = targetFormat.format(date); 
        
        
        System.out.println(formattedDate);*/
```
